import { FindCoursePipe } from './find-course.pipe';

describe('FindCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new FindCoursePipe();
    expect(pipe).toBeTruthy();
  });
});
